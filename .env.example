# MCP服务配置示例 - HTTP API版本

# 背景移除MCP服务配置 (HTTP API)
MCP_BACKGROUND_REMOVER_ENDPOINT=http://localhost:3001/api/remove-background
MCP_BACKGROUND_REMOVER_METHOD=ai_model
MCP_BACKGROUND_REMOVER_TIMEOUT=30000

# 其他MCP服务配置（团队可扩展）
# MCP_IMAGE_UPSCALER_ENDPOINT=http://localhost:3002/api/upscale-image
# MCP_IMAGE_ENHANCER_ENDPOINT=http://localhost:3003/api/enhance-image

# 注意：
# 1. 将此文件重命名为 .env 并放置在项目根目录
# 2. 根据团队开发的实际MCP HTTP API端点进行配置
# 3. 确保MCP服务支持CORS以允许前端访问
# 4. MCP HTTP API应该接受POST请求，Content-Type为application/json
# 5. 在本地开发时，确保MCP服务已启动并监听指定端口
